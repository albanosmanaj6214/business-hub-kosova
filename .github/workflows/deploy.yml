name: Deploy

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: [self-hosted, businesshub]
    steps:
      - name: Run deploy script
        run: sudo -n /usr/local/bin/deploy-businesshub.sh
